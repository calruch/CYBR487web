# How it works

This page should explain the end-to-end scan flow (arg parsing → host discovery → port scan → traceroute → reporting).

!!! note "Placeholder"
    If you want, paste your current `--help` output and I’ll align this explanation exactly to it.
