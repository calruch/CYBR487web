# API reference

This page is generated automatically during the build.

If you see an error like “mkdocstrings could not find module”, ensure:
- your code is under `src/`
- (recommended) `src/__init__.py` exists
