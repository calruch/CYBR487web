# CLI reference

This is derived from the snapshot’s argument parser behavior.

| Flag | Meaning |
|---|---|
| `-n/--network` | Target network in CIDR |
| `-p/--ports` | Ports list or range |
| `-t/--timeout` | Timeout seconds |
| `-v` | Verbose |
| `-vv` | More verbose |
| `-s/--hostid` | Host discovery method |
| `-m/--maxhops` | Traceroute hops |
| `--scanType` | Scan mode |

!!! note "Placeholder"
    If your parser differs, update this table to match `--help` output in your repo.
